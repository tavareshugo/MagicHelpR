.onLoad <- function(libname, pkgname) {
	packageStartupMessage("The `MagicHelpR` package is not actively maintained or developed. We advise using the R/qtl2 package instead, which offers all the same functionality (and more): https://kbroman.org/qtl2/")
}
