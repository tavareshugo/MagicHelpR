---
title: "MagicHelpR introduction"
author: "Hugo Tavares"
date: "27 September 2016"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Introduction to MagicHelpR}
  %\VignetteEngine{knitr::rmarkdown}
  \usepackage[utf8]{inputenc}
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
devtools::load_all('~/Documents/code/R_packages/MagicHelpR/')
setwd('~/temp/magic_test_27/')
downloadArabMagic('~/temp/magic_test_27', example_data = T)
phen <- read.table('magic_phenotype_example.txt', header = T)

magicgen <- magicFounderReconstruct('./all_chr.MAGIC.happy.data', './all_chr.MAGIC.happy.map', './all_chr.MAGIC.happy.alleles')

magicgen2 <- addPhenotypes(magicgen, phen, "SUBJECT.NAME")

boltqtl <- magicQtlScan(magicgen2, "days.to.bolt")

ggplot(boltqtl, aes(bp/1e6, -log10(p))) +
  geom_line() +
  facet_grid(~ chromosome, scales = "free_x", space = "free_x") +
  geom_hline(yintercept = 3.5)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
